# O pyproject.toml é a fonte única das dependências fixadas.
# Mantido para o fluxo do setup.sh e do README:
#
#   pip install --no-compile -r requirements.txt
#
# A flag --no-compile é necessária por causa do wheel do MediaPipe (ver README).
-e .
